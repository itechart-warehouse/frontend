export interface Values {
  userEmail: string;
  firstName: string;
  lastName: string;
  birthDate: string;
  address: string;
  company_id: string;
  role_id: string;
  userPassword:string;
}

export interface Roles {
  id: number;
  name: string;
}
